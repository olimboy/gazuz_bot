version: "3"

services:
  svetuz_bot:
    image: svetuz_bot
    container_name: svetuz_bot
    build: ./
    restart: always
    volumes:
      - ./:/app
    command: python manage.py migrate
networks:
  default:
    external:
      name: nginx-proxy